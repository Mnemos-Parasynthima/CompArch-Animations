- Show array rule in action
	- One struct instance takes up "one" byte in `MemoryBlock`
	- Line up as many structs as wanted
- Show some "failures"/wrong setups
	- Has misaligned data
- Maybe show with different starting addresses
- Include `Union()` in `structs.py`